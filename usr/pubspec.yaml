name: hleduroom_app
description: "H.L.-Eduroom Production App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Core WebView
  webview_flutter: ^4.7.0
  # Navigation & External Links
  url_launcher: ^6.2.5
  # State Management
  provider: ^6.1.1
  # Offline & Storage
  connectivity_plus: ^5.0.2
  shared_preferences: ^2.2.2
  sqflite: ^2.3.2
  path: ^1.9.0
  path_provider: ^2.1.2
  # UI
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.0
  # Downloads
  flutter_downloader: ^1.11.0
  permission_handler: ^11.3.0
  # Firebase (Placeholders for production)
  # firebase_core: ^2.27.0
  # firebase_auth: ^4.17.8
  # cloud_firestore: ^4.15.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
